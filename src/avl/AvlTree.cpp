#include <iostream>

using namespace std;

class Node {
    int data;
    Node *left;
    Node *right;
    int hight;


    Node(int key) {
        data = key;
        left = nullptr;
        right = nullptr;
        hight = 1;
    }
};

Node *insert(Node *root, int key) {
    
}

int main() {
    return 0;
}
