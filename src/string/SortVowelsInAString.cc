#include <iostream>

using namespace std;

string sortVowels(string s) { return s; }

int main() {
  string s = "lEetcOde";
  cout << sortVowels(s);

  int ch = 'a';
  cout << ch << endl;
  return 0;
}
