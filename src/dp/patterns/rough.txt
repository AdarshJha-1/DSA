1. What does my state mean? -> dp[i] ?? dp[i][j]?? 
2. From which states can I reach this state? -> dp[i][j] = max(dp[i.]...) or dp[i] = dp[i - 1] + dp[i - 2] ??
3. What operation combines those possibilities?
4. In what order can I calculate the states?


vector<int> dp(n + 1, 0);
dp[1] = nums[0];

for(int i = 2; i <= n; i++)
{
    for(int j = i - 2; j >= 0; j--)
    {
        if(nums[i - 1] > nums[j])
        dp[i] = max(nums[i - 1] + dp[j])
    }
}